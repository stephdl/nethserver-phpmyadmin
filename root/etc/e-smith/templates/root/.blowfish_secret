{
    #----------------------------------------------------------
    #  This is needed for cookie based authentication to encrypt 
    #  password in cookie for phpmyadmin (blowfish_secret)
    #----------------------------------------------------------

    use esmith::util;
    my $pw = esmith::util::genRandomPassword('/root/.blowfish_secret');
}
